<Window x:Class="OSL.WPF.View.MainWindow"
        xmlns:Views="clr-namespace:OSL.WPF.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OSL.WPF.View"
        mc:Ignorable="d"
        Title="OpenSportLog" Height="600" Width="800"
        DataContext="{Binding MainWindow, Source={StaticResource Locator}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="0">
            <Menu Name="mnuMain" DockPanel.Dock="Top" IsMainMenu="True">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open" Command="{Binding OpenFileCommand}" IsEnabled="{Binding Path=IsOpenFileEnabled}"/>
                    <MenuItem Header="_New" Command="{Binding NewFileCommand}" IsEnabled="{Binding Path=IsNewFileEnabled}"/>
                    <MenuItem Header="_Save" Command="{Binding SaveCommand}" IsEnabled="{Binding Path=IsSaveFileEnabled}"/>
                    <Separator/>
                    <MenuItem Header="New _Athlete" Command="{Binding NewAthleteCommand}" IsEnabled="{Binding Path=IsNewAthleteEnabled}"/>
                    <Separator/>
                    <MenuItem Header="_Exit" Command="{Binding ExitCommand}" IsEnabled="{Binding Path=IsExitEnabled}"/>
                </MenuItem>
                <MenuItem Header="_Import">
                    <MenuItem Header="_FitLog (SportTracks)" Command="{Binding ImportFitLogCommand}" IsEnabled="{Binding Path=IsImportEnabled}"/>
                </MenuItem>
            </Menu>
        </DockPanel>
        <Views:AthleteDetails Grid.Column="0" Grid.Row="1"/>
        <GridSplitter Grid.Column="0" Grid.Row="2" Height="5" HorizontalAlignment="Stretch" />
        <Views:ActivityDetails Grid.Column="0" Grid.Row="3" IsEnabled="{Binding Path=IsAthleteSelected}"/>
    </Grid>
</Window>
